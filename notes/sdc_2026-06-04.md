add the scene and cloud coverage into the binning process as well and total wavelength into the coefficient generation

big sem ver will be 0.x.x till launch

- I have cloud coverage as binary currently?
- thats ok for current data, will need to define threshold when we start using updated modtran data
    - but keep binary

Someone else needs a realistic unfiltered data file from us

- timeline is mid to late june
    - end of june we need to be able to make a realistic data product
    - need to be able to scientifically validate the data that it is good
- we want a switch to flip between standard method and multivariate

0.1.0 - beginning basic delivery 

0.2.0 - science accuracy

0.3.0 - internals

need to define the radiance vars in the yaml file as well

-ssw

-sw

-tot

-lw

0.1.0 reqs

- sep coeff file
- repeatable coeff gen
- prod coeff usage
- prod data product
- prod data dep
    - input files will be radiometer data, coefficients and scene id file
    - use these to create a standard output

So the arch is that I will get the scene id  (IGBP surface type) which I need to filter down into the 5 different scene types I currently use, then have the coefficients generated off scene_id, cloud, sza, vza, raz. So its a 5d input, we can think of it like a cube where these track to the index where the coefficients lay (I think). We need multivariate and univariate in different cubes right now.